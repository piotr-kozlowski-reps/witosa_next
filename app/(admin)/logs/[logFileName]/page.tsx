type Params = {
  params: Promise<{
    logFileName: string;
  }>;
};
export default function LogFileContent(params: Params) {
  //TODO: logFile type!!!
  //use getLogFileContent function to fetch data
  return <div>log file inside:</div>;
}
