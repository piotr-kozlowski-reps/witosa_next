import { TEventTemporary } from '@/types';
import { describe } from 'vitest';

const mockEvents: TEventTemporary[] = [];

describe('getNewsDataSorted()', () => {
  // const
  // it('returns 00', async () => {
  //   expect(await getNewsDataSorted()).toHaveLength(2);
  // });
});
